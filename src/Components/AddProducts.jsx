import React from "react";

const AddProducts = () => {
  return (
    <div className="h-screen flex justify-center items-center w-full ">
      <section className="w-full md:w-[70%] bg-white rounded-lg ">
        Add Products
      </section>
    </div>
  );
};

export default AddProducts;
